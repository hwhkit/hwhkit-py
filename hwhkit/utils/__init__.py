"""hwhkit.utils — general-purpose utility module.

Submodules:
- ``hash``: SHA-256 / bcrypt helpers
- ``encryption``: AES-256-GCM encrypt/decrypt
- ``decorators``: @retry / @timed / @safe_execute
- ``http``: async httpx client wrapper with sane defaults
- ``notification.feishu``: Feishu webhook Notifier (Notifier contract)
"""

__all__: list[str] = []
